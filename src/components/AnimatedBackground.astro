<div class="absolute inset-0 -z-10 overflow-hidden">
  <div class="blob blob-left" id="blob-left"></div>
  <div class="blob blob-right" id="blob-right"></div>
</div>

<style>
  .blob {
    position: fixed;
    top: 0;
    height: 100vh;
    width: 40vw;
    border-radius: 50%;
    filter: blur(120px);
    opacity: 0.5;
    z-index: -1;
    background-color: #c6b79b;
  }

  .blob-left {
    left: -40vw;
    background: radial-gradient(circle at center, #8c7a5b7a, #8c7a5b7a);
  }

  .blob-right {
    right: -40vw;
    background: radial-gradient(circle at center, #8c7a5b7a, #8c7a5b7a);
  }

  @keyframes blob {
    0%,
    100% {
      transform: translate(0, 0) scale(1);
    }
    33% {
      transform: translate(30px, -50px) scale(1.1);
    }
    66% {
      transform: translate(-20px, 20px) scale(0.9);
    }
  }

  /* utility for staggered animation */
  .animation-delay-2000 {
    animation-delay: 2s;
  }

  .animation-delay-4000 {
    animation-delay: 4s;
  }
</style>
